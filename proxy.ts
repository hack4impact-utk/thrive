import { NextRequest, NextResponse } from "next/server";

export function proxy(request: NextRequest): NextResponse {
  // if user is signed in and info filled is false and page is not info page
    // redirect to info page
}
